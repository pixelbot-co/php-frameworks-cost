# Generated by nginxconfig.io
# https://www.digitalocean.com/community/tools/nginx#?0.domain=_&0.path=%2Fphp&0.https=false&0.index=index.html&0.access_log_domain&0.error_log_domain

user www-data;
pid /run/nginx.pid;
worker_processes 1;
worker_rlimit_nofile 65535;

events {
    multi_accept on;
    worker_connections 65535;
}

http {
    charset utf-8;
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    server_tokens off;
    log_not_found off;
    types_hash_max_size 2048;
    client_max_body_size 16M;
    keepalive_requests 65535;
    keepalive_timeout 10;
    client_body_timeout 10;
    send_timeout 10;

    # MIME
    include mime.types;
    default_type application/octet-stream;

    # logging
    access_log off;
    #/var/log/nginx.access.log;
    error_log /var/log/nginx.error.log warn;

    # load configs
    include /etc/nginx/conf.d/*.conf;
    include /etc/nginx/sites-enabled/*;
}

#daemon off;
