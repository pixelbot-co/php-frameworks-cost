SERVER NODE
http://157.245.220.205/slim/4.3.0/

# Docker
apt-get update
apt-get install apt-transport-https ca-certificates curl software-properties-common
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu bionic stable"
apt-get update
apt-cache policy docker-ce
apt-get install docker-ce

CLIENT NODE
157.245.179.215

# wrk
apt-get install build-essential libssl-dev git -y
git clone https://github.com/wg/wrk.git wrk
cd wrk
make
cp wrk /usr/local/bin

# pft
git clone https://github.com/pixelbot-co/php-frameworks-cost.git pft
cd pft/client
docker build -t docker_client .


# php
apt-get install php7.2-fpm php7.2-curl

